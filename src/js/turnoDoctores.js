function horarioDoctores() {
    const options = document.querySelectorAll(".Lunes").addEventListener("click");
    const horarios = {};
    options.forEach((hora) => {
        horarios[dato.id] = dato.value
    });
    console.log();
}
